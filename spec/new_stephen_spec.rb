require "spec_helper"

describe NewStephen do
  it "has a version number" do
    expect(NewStephen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
